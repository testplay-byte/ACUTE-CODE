# tests/

Placeholder for end-to-end tests (Phase 4+): driving the packaged app or the
Tauri dev shell — project creation, session runs against a stubbed provider,
approval dialogs, and update flows. Unit and component tests live next to the
code they cover (`src/`, `agent-core/tests/`, `shared/src/`) and run via
`pnpm test`.
