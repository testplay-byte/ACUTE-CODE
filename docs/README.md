<!-- last-reviewed: 2026-09-09 round-80 -->
- [ui-iterations/round-80.md](ui-iterations/round-80.md) — the R80 reliability round (the owner's four-clause field report: "the chat ends without any error message or anything some times" / "for error messages raw messages should be shown too" / "in the settings retry customization is needed" / "make sure it works with the nvidia api key too"): (1) the SILENT STOPS fixed at three root causes — the SSE clean-close truncation guard (chat.ts: zero finish-step parts + content deltas = the honest "connection closed mid-response" throw, network-classed so the retry ladder engages, the partial text kept by the R75 flush), the context/request guard stops (runtime.ts: CONTEXT_LIMIT/REQUEST_LIMIT codes — the LOOP_GUARD pattern, persisted turn.error + usage + 502 + the session back to queued, never the pre-R80 ok:true), and the route-crash persistence (server.ts: best-effort persistTurnError + task_failed, crash-guarded — a crash no longer vanishes on reload); (2) the RAW ERRORS — providerErrorDetail 500→4000, userMessage 240→600, the connection-test scrub 500/160→4000/2000, the debug-analyst slice 300→2000, key-scrub unchanged + nvapi- joined the redaction surfaces; (3) the RETRY CUSTOMIZATION — RetrySettings gained maxAttempts (2–10) + waitMinutes (per-rung, 0–1440) + providerTimeoutSeconds (60–3600), validated at route + storage, resolved defensively per turn (resolveRetrySchedule — defaults byte-identical to the R75 ladder), the Settings General card's schedule section (stepper + per-rung inputs + timeout + Reset), the task_failed schedule line from the real settings; (4) the NVIDIA NIM provider — seeded (integrate.api.nvidia.com/v1, OpenAI-compatible), ACUTE_PROVIDER_NVIDIA env injection (keys.rs + dev.mjs's ~/.acute/nvidia.key), the Settings preset, live-verified reachable + catalog + key auth (the account's model functions EOL'd server-side — the raw bodies show verbatim). The session continued an in-flight tree from the context break — verified claim-by-claim (lesson #82), one typing bug + five stale-pin classes fixed honestly (the R58/R78 stream mocks now model the healthy wire: a finish-step part; AGENT-MEMORY lesson #83). Root 2772 in 149 files (agent-core 1796 incl. the four new r80 suites' 27 tests, frontend 964, e2e 12), live on the real keys (a real OpenRouter streamed turn end-to-end, the retry PUT round-trips, the NVIDIA connection test).
- [ui-iterations/round-79.md](ui-iterations/round-79.md) — the R79 orchestrator round (the standing R73 deferral: delegate_task task_id/background/resume — addressable, background, resumable sub-agent tasks, the last unshipped piece of the R71 sub-agent discipline; no new field report — the session ran on the planning/backup instruction): (1) the tool's three call shapes — BLOCKING (verbatim pre-R79, optionally + task_id), BACKGROUND (task + task_id + background:true — REQUIRES task_id; the immediate receipt while the child runs DETACHED through the same runChildTurn machinery, best-effort emit because a background child routinely outlives the parent's SSE, the parent-abort cascade forwarded so a Stop leaves no zombie spend), and COLLECT (resume by task_id → session id → 4-char code: waits while running, returns the report idempotently, retries a failed child via the ADR-0022 §3 continuation, unknown → the honest addressable list), with honest refusals (grammar ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$, duplicates naming the status, the ≥10-outstanding fan-out cap, background-without-task_id, neither→the list) and the tool NEVER throwing; (2) the delivery channel — a per-turn "## BACKGROUND TASKS" system-prompt section (the taskHints ephemeral pattern; only UNCOLLECTED addressable children; live status lines; the 10-row cap; the cheap indexed guard so childless sessions compose byte-identically; prompt-registry 23→24) + the NEW delegation.collected event on the parent's log (blocking+task_id at completion — inline delivery; background at resume; skipped by assembleHistory and the frontend fold by construction); (3) persistence — migration 0028 (sessions.delegate_task_id + idx_sessions_parent_task, NULL default = pre-R79 behavior) with taskId on the /subagents rows and every subagent-status frame; (4) the frontend — the panel header's mono task chip (subagent-taskid-chip, beside the code chip, tooltip teaching delegate_task {"resume":"…"}) + the chat card's chip (subagent-card-taskid). Root 2741 in 145 files (agent-core 1769 incl. the new 27-test r79-delegation suite, frontend 960 incl. +6, e2e 12), battery-r79 10/10 (the request-body oracle: the model SEES the reminder section, and it disappears after collection; the failed-child "You were interrupted" retry continuation). ADR-0028 records the decision; AGENT-MEMORY lessons #81 (the mock-provider oracle discipline) + #82 (an interrupted agent's tree is a claim).
- [ui-iterations/round-78.md](ui-iterations/round-78.md) — the R78 honest-errors + queue round (the owner's four verbatim asks): (1) the error chain robustified — the AI SDK's RetryError is unwrapped before classification (extractStatus walks lastError/errors, depth ≤ 4), a 403 whose body says region/moderation/permission/unavailable reclassifies to fail-fast unknown carrying the REAL text (a region block no longer claims "authentication failed"), userMessage/classMessage are the provider's REAL words (240-char cap, generic lines demoted to the empty fallback), and providerError rides every meta.retry frame + the 502 envelope + turn.error so the LIVE RetryStatusCard shows the class chip + the API's actual text (mono, 3-line clamp, the R77 expand toggle) — only a REAL rate limit ladders; (2) the composer's action group restructured into a never-wrapping bottom-right anchor (a wrapping area for the left cluster + ml-auto selectors, then data-composer-actions as a shrink-0 justify-between sibling holding Continue/Send/Stop/queue-send — the bottom buttons can never render in a wrong position; browser-verified stable at 386/480/560/641/900 px); (3) the Settings → General retry config (RetrySettings {autoRetryRateLimit, autoRetryTimeout, autoRetryNetwork} behind GET/PUT /settings/retry; the ladder gated per class — a disabled class fails fast with the real text; the tab LABEL is General, URL id 'advanced' unchanged); (4) the message queue — a message.queued event (delivery is a strict type flip so the message lands where it was queued), loop-top delivery before assembleHistory (the model reads the queued text WITH the completed tool results; a ladder wait delivers at the next rung), the turn-end same-stream continuation loop (meta.queue_continue, first consumed + rest flipped, task_complete per turn, capped at 25), the pre-flip crash/stop recovery, POST/DELETE /sessions/:id/queue (+ the user.queued frame bridged onto the open stream), and the frontend (queue-send beside Stop, amber chips with X-remove/Send-now, the queued fold item). AGENT-MEMORY lessons #79 (never paraphrase the provider's error into a class line) + #80 (agent-browser open is a hard reload; unwrap the SDK RetryError). Root 2708 in 144 files (agent-core 1742, frontend 954, e2e 12), battery-r78 12/12 (a mock provider recording request bodies as the model-facing oracle), browser-verified on the real 429 storm.
- [ui-iterations/round-77.md](ui-iterations/round-77.md) — the R77 field-report round (the owner's seven verbatim asks): (1) the composer toolbar split LEFT (attach + access + task mode) / RIGHT (context donut + model + reasoning + send) with the right cluster a nowrap ml-auto group so the send/stop action NEVER wraps below alone; (2) the model pill leads with a Cpu icon (icon-only below the 560px container floor); (3) the RetryStatusCard redesigned — circular spinner chip, six-dot attempt ladder with the breathing current dot, a countdown fill-bar driven by waitMs, rounded class pill; (4) the vanish fix — a turn that fails WITHOUT a persisted turn.error (pre-hijack rejections, dropped streams) keeps the error card + the optimistic user bubble (NEW stream-store freezeFailedTurn; the old flow wiped both, which is what 'not sending the message properly' actually was); (5) API error envelopes arrive intact (the real code + details ride the error frame) and TurnErrorCard grows a Show-full-error toggle for the complete raw provider text; (6) revert-to-message is edit-and-resend — the backend truncates FROM the target message inclusive (seq >= keepThroughSeq) and the composer refills with the message's text; (7) the live real-provider E2E battery (smoke, static site, python CLI + self-run tests, 3-turn build/extend/refactor, 10-turn conversation with retention probes, revert over the wire, error surfacing) + a genuine account-wide free-tier 429 storm that live-verified the R75 ladder (frames at 90s/5m/10m/30m, ~47-min worst case), the amber card, and the boot sweep's INTERRUPTED event. 2623 + 1686 tests green.
- [ui-iterations/round-76.md](ui-iterations/round-76.md) — the R76 documentation round (the owner: "update the documentation… everything up to date and well managed… accurate and properly structured"): the audit found the truth docs had drifted behind the code — ORCHESTRATION-WORKLOG (the session history HANDOFF §1 points every new agent at) stopped at R67 (backfilled R68–R75, pure append +147 lines, source conflicts resolved in git's favor); AGENT-MEMORY stopped at lesson #71/R58 (five lessons appended, #72–#76); ARCHITECTURE's crash-recovery described a shutdown route that doesn't exist + the pre-R75 terminal-failed sweep (rewritten to the INTERRUPTED/queued truth; the module map gained the R75 modules + the WS-never-built note); IMPLEMENTED-API refreshed R73→R75 (26 tools, /modes readOnly, meta.retry, attempts, the ROUND-75 section); EXTENSIBILITY gained the enforcement addendum; DESIGN-SYSTEM's composer anatomy was pre-R50 (rewritten); ROADMAP unfrozen from round-17; the index gained 5 unindexed runbooks; the two missing R75 ADRs written (0026 task-mode tool policy + 0027 the retry-ladder composition — the decisions were load-bearing with no record); MAINTENANCE recipe g step 2b + DOC-STANDARDS §8 carry the stamp-cohort rule; SECURITY control 7; the HANDOFF/AGENTS/SPEC/README model + transport + commit truths fixed. Two research agents (the 20-item edit list verified against code; the backfill), docs-only (no code, no version bump, no release), docs:check 173/0/0, 36 milestones.
- [ui-iterations/round-74.md](ui-iterations/round-74.md) — the R74 updater-freeze round (the owner's report: "the installed desktop app version was 0.67.0 — it did not update"; the first launcher code change since R63, ACUTE.bat byte-identical): root cause PROVEN against the live GitHub API — the /releases list sorts never-published DRAFTS above every published release, the R63–R67 close-outs left five drafts (v0.63.0–v0.67.0) at positions 1–5, and the launcher's first-match walk returned draft 0.67.0 as "latest" → the installed 0.67.0 looked current while 0.73.0 was live (no error; it "used to work" because in that era ALL releases were drafts); the fix — NEW pure _pick_latest_release (max version over the whole list, integer tuples so 0.10.0 > 0.9.0, drafts first-class via the owner PAT, same-version tie prefers published, malformed entries skipped) + _desktop_latest_release rewritten onto it (per_page 20→100, one retry, never-raise/dev-fallback byte-identical) + both call sites + the version-truth panel NAMES the tag it picked (and says draft) + ACUTE.bat status annotates published/draft; NEW launcher/tests/test_pick_latest_release.py — the repo's first Python suite (stdlib unittest, 10 tests incl. the exact owner-freeze list shape) wired into ci.yml's verify job AND release.yml's launcher-kit gate (github-release needs the kit job → every tag gated); remediation — the five stale drafts published (releases 381461843/381577121/382282125/383173421/383247624, all HTTP 200, non-destructive) which also un-freezes pre-fix launchers (settled list carries v0.73.0 first); the fix reaches the owner via the launcher's own self-update (git pull → copy + re-exec); anomaly flagged: v0.68.0 never existed (R68 skipped the tag push); root 2558 unchanged + re-run green, launcher suite 10/10, live smoke ×2 (orchestrator + the independent R74-b verifier, 9/9 PASS, verdict SHIP)
- [ui-iterations/round-75.md](ui-iterations/round-75.md) — the R75 reliability & enforcement round (the owner's four verbatim asks): (1) the TRANSIENT-API RETRY LADDER — rate_limit/network/timeout failures retry immediately then wait 1.5/5/10/30 min (six attempts) before stopping+notifying+showing the error, with the live amber Retrying status card + countdown; auth/overflow/unknown never auto-retry; (2) TASK MODES ARE NOW HARD-ENFORDED — plan/review/explore intersect the toolset to read-only (write tools REMOVED, the prompt never lists them), debug demotes run_command to the auto tier, children inherit the parent's mode, read-only modes are owner-pinned against switch_mode escapes — live-verified: a plan-mode session asked to write a file honestly reported its read-only toolset and nothing was written; (3) mode menus are single-line with descriptions on hover + read-only badges; (4) the composer toolbar is ONE row in the owner's exact order (container-query icon collapse below 560px) and the textarea ACTUALLY auto-grows now (flex-1 was silently ignoring the height style — the never-worked 'two lines' bug; 5 visible lines then scroll). The round's deepest find: the AI SDK delivers post-retry provider errors as fullStream error PARTS the adapter ignored — real 429s classified 'unknown' and could never ladder; the adapter now re-throws them (live-verified against a mock 429 provider: meta.retry frames on the wire, exact schedule). Plus: the sync-path ok:true sub-agent lie fixed, the crash sweep writes the INTERRUPTED event, errorClass+attempts reach the cards. 2613 tests (2558+55) green, 2 new suites (19+27), browser-verified end-to-end.
- [ui-iterations/round-73.md](ui-iterations/round-73.md) — the R73 task modes round (the posture tier of the owner's "proper detailed system prompts which the agent accesses when required and on the basis of the task" directive): six builtin POSTURE modes plan/debug/build/review/explore/refactor (NEW agents/modes.ts — deep 2.0-2.5KB bodies with iron laws + PAIRS WITH skill pairings; the debug body is pure posture with an Escalation section) + custom modes from .acute/agents/*.md (frontmatter name/description/tools, ≤8 × 16K caps, 8 diagnostic kinds, custom shadows builtin, project-root trust); the backend — prompt-registry 21→23 (task-modes index + active-mode deep body, both after skills, strictly ctx-gated: golden fixture BYTE-IDENTICAL), computeModeHints (the R72-a shared scorer; computeTaskHints unchanged), sessions.active_mode + migration 0027, PATCH /sessions/:id activeMode (unknown → 400 with available ids) + GET /projects/:id/modes metadata-only, NEW switch_mode tool (activate-returns-body-once/deactivate, the schema-clean { mode: "none" } sentinel taught), custom-mode tools narrow the allowlist narrow-only, PLAN_MODE_TOOLS gains switch_mode; the frontend — the composer TaskModePicker (ModeSwitcher's mirror) + the /mode slash intercept (word-boundary, id-or-name case-insensitive, optimistic PATCH + rollback) + api.ts TOOL_CATALOG switch_mode (the R45 drift guard repaired); the generalized system-reminder renderer (ONE fenced mechanism; r72-dir-conventions 20/20 unmodified — the byte-identity proof) + ReminderBudget; skills 18→20 (spec-planning, performance); 2558 root tests (+135)
- [ui-iterations/round-72.md](ui-iterations/round-72.md) — the R72 adaptive capability round (the owner's "on the basis of the task" directive): deterministic task→skill hints (NEW agents/task-hints.ts — a no-LLM, zero-cost, zero-latency matcher scoring the turn's user message against the effective skills' trigger-rich descriptions: quoted phrases ×5, stopword-filtered tokens +1, top-2 above threshold) rendering one advisory "Task signal: this request looks like it matches **<name>** — consider calling read_skill FIRST" line in the SKILLS prompt section (both turn paths, per-turn ephemeral, flows through the shared resolver — computer-use gate + agent allowlist; the golden fixture BYTE-IDENTICAL, ctx purely additive); skills 12→18 (six engineering crafts: tdd, api-design, frontend-craft, typescript-craft, security-review, refactoring — sortOrder 12-17, trigger-rich descriptions 447-490 chars, bodies 1.6-1.9K with iron laws + output blocks); references/ depth for file skills (the Agent-Skills standard: one level, ≤8 .md ≤64KB; read_skill's optional `reference` param + the copy-pasteable listing block; GET /skills metadata additive); per-directory conventions (NEW tools/dir-conventions.ts — read_file appends the capped 2K "[conventions from <dir>/AGENTS.md]" fenced reminder from the deepest AGENTS.md/CLAUDE.md strictly below root, root skipped, once per session+dir); 2423 root tests (+136)
- [ui-iterations/round-71.md](ui-iterations/round-71.md) — the R71 discipline & reliability round (research-driven from the owner's four reference repos — karpathy-skills, alirezarezvani/claude-skills, cline, kilocode; the R71-c audit found the gaps in the model-facing layer): the ENGINEERING DISCIPLINE prompt section (4 karpathy principles with binary self-tests, [KNOWN]/[ASSUMED]/[UNKNOWN] tagging with never-code-on-an-UNKNOWN, 3-strike escalation, the red-flags table) + task→verifiable-goal transforms in PLAN + verification receipts + 🟢🟡🔴 confidence tags + devil's advocate in COMMUNICATION + sub-agent scope/no-polling; tool reliability — read_file/run_command truncation markers carry the exact next call ("use offset=N to continue", spill-to-file recovery), edit_file consecutive-failure escalation (2nd re-read / 3rd change-approach / 5th refuse — NEW edit-streak.ts), owner denials are "NOT a tool or system failure" with timeouts distinguished, six-class provider-error classification riding the 502 envelope + turn.error event, context-overflow recovery (forced compaction + one retry, visible system line) on both turn paths; the skills round — 8 builtin descriptions rewritten trigger-rich ("Use when [phrasings]. NOT for [adjacent case]") + focused-fix/zero-hallucination/self-eval/ship-gate (12 builtins); golden 20,156→23,665 bytes; 2287 root tests (+110)
- [ui-iterations/round-70.md](ui-iterations/round-70.md) — the R70 agent brain round (research-driven — the round's own prompt audit + the OSS agent study: the model now knows its world, follows conventions, and verifies before claiming done: env grounding — the real OS/shell/date/git branch+dirty in every turn's prompt, TERMINAL teaches only the real platform's syntax; the AGENTS.md/CLAUDE.md/AGENTS.override/CLAUDE.local convention ladder auto-loads with source markers + @file imports (16K/32K caps); the four overlapping planning sections merged into ONE five-phase loop (registry 23→20 ids); the Codex dirty-worktree rule + verify-after-edit contract + path:line communication; the ACI round — run_command keeps head 32KB AND tail 32KB (errors live at the end), read_file is cat -n + offset/limit, honest no-output messages; file-based skills (.acute/skills + ~/.agents/skills, the Agent-Skills standard, DB-row precedence, 409 refusals) + SEVEN new builtins (incl. project-init — writes the project's AGENTS.md — and browser-use) + STICKY skill bodies (60K budget, no replay stub — loaded instructions survive the task) + agent.skills wired; prompt 22,460→20,156 chars while ADDING all of it)
- [ui-iterations/round-69.md](ui-iterations/round-69.md) — the R69 enforcement layer (the residuals R68's own close-out named: every mutating action returns an OBSERVATION receipt — fresh frame + screenChanged + focusedElementName + activeApp title after a 600 ms settle, returnState compact/none/full; coordinate clicks verify changed/unchanged + hitElementName; the pngjs aHash frame memory + provenance; stale-frame AUTO-refresh (frame_changed carries the already-registered fresh frame — the frame_stale dead-end dies for pointer tools); the screenshot-spam guard (screen_unchanged refuses the 3rd near-identical capture); wait() observable; middle/right element clicks; observation frames inline in the chat; the Windows scroll-math fix (the quadratic over-scroll) + HWHEEL + the stdin clipboard paste for long typing + the dual-object Chromium poke; the NOACTIVATE monitor that never steals focus; the prompt/skill re-teach — pngjs@7 MIT, 134 deps CLEAN)
- [ui-iterations/round-68.md](ui-iterations/round-68.md) — the R68 computer-use overhaul (the 0.67.0 field report: the inline screenshot rows at the capture moment — the strip deleted; the WDA_EXCLUDEFROMCAPTURE monitor invisible to the agent's own captures; the SendInput input-path rewrite + the ARGV ceiling guard; the self-healing foreground + the activate ladder; the Chromium poke making Edge's web tree searchable; the 30 s frames; the vision 429/5xx retry; the prompt/skill/description teaching — web browse confirmed working, untouched)
- [ui-iterations/round-67.md](ui-iterations/round-67.md) — the R67 bridge round (the 0.66.0 field report fixed end-to-end: the instant navigate frame + create-on-adopt, the WebView2 eval double-parse, the chat-session tab binding + per-project cookies, the attachment upload pipeline, the debug card's copy + the full-turn export, the -EncodedCommand capsules + the SendKeys key tool + the monitor turn-holds, the live screenshot thumbnails)
- [ui-iterations/round-66.md](ui-iterations/round-66.md) — the R66 live-fire patch (the 15-action browser page actions + form submission; the owner-solvable bot-wall checkpoint; the instant viewport apply; the vision split + analyze_image; the post-turn context-free debug analyst; the Windows find_elements + 2400 walk; the top-center 460×56 monitor + the decayed live signal; the settings rail)
- [ui-iterations/round-65.md](ui-iterations/round-65.md) — the R65 honesty patch (the SURFACE BOUNDARY prompt lines — embedded browser ≠ real desktop; the browser tab auto-open on agent browsing; debug mode with the execution-report switch; the Advanced cleanup + the search-engine allowlist)
- [ui-iterations/round-64.md](ui-iterations/round-64.md) — the R64 capability round (the Windows computer-use fixes: JSON array collapse + EnumWindows + tiered app resolution; the always-on-top floating monitor; the chat polish backlog; per-key usage + the BPE-style estimator)
- [ui-iterations/round-63.md](ui-iterations/round-63.md) — the R63 desktop-update round (release-tag gap closed + the launcher's version-truth chain: registry · exe · engine, uninstall/reinstall, sha256-verified downloads)
- [ui-iterations/round-62.md](ui-iterations/round-62.md) — the R62 owner-feedback round (sidebar rail, layout, browser truth + agent-browser)
# ACUTE-CODE — Documentation Index

One line per document. **Reading order for a new session:** `HANDOFF.md` →
`docs/runbooks/WORKFLOW.md` → `docs/runbooks/AGENT-MEMORY.md` → the runbook
for the task at hand. Generated files are marked; everything else is
hand-maintained. If a doc contradicts the code, the code wins for facts —
file the discrepancy and fix the doc in the same round.

## Process & memory
- [`runbooks/WORKFLOW.md`](runbooks/WORKFLOW.md) — **the session spine**: start ritual, branch policy, verification gates, per-round documentation duties, push/CI/ntfy checklist
- [`runbooks/DOC-STANDARDS.md`](runbooks/DOC-STANDARDS.md) — **the docs contract**: canonical structure, the last-reviewed stamp + `docs:check` CI gate, ADR/runbook/round-file templates, the stamp-cohort refresh rule (R75)
- [`runbooks/ORCHESTRATOR-METHOD.md`](runbooks/ORCHESTRATOR-METHOD.md) — the orchestrator's cognitive workflow (R28)
- [`agent/REVIEW-CADENCE.md`](agent/REVIEW-CADENCE.md) — review pacing (R28)
- [`runbooks/PROMPT-MODULES.md`](runbooks/PROMPT-MODULES.md) — per-project system-prompt section overrides (R59, the sixth extension surface)
- [`runbooks/CLI-HARNESS.md`](runbooks/CLI-HARNESS.md) — the acute.mjs CLI chat harness (R58)
- [`runbooks/MAINTENANCE.md`](runbooks/MAINTENANCE.md) — **how to find things and change things safely**: architecture map + how-to-add-tool/migration/settings-tab/sidebar-tab/route/screen recipes + the golden rules (R44)
- [`runbooks/ROADMAP.md`](runbooks/ROADMAP.md) — milestones per pillar with status (`done / in-flight / queued / owner-gated`); the single place HANDOFF §9 points to
- [`runbooks/TESTING.md`](runbooks/TESTING.md) — the five verification layers, the fresh-DB rule, live-battery and browser-verification recipes
- [`runbooks/SECURITY.md`](runbooks/SECURITY.md) — security posture: bearer/loopback, tool path containment, secrets custody per surface, launcher redaction, the R45 child-env/auto-tier/web-gate controls, known gaps
- [`runbooks/AGENT-MEMORY.md`](runbooks/AGENT-MEMORY.md) — numbered lessons (mistake → root cause → rule); read before any session; append-only
- [`runbooks/PROJECT-MEMORY.md`](runbooks/PROJECT-MEMORY.md) — the R44 agent memory SYSTEM reference (table, tools, digest injection, Memory tab, verification pattern; relevance-ranked recall/digest + dedup-on-save since R46) — distinct from the lessons file above
- [`agent/ORCHESTRATION-WORKLOG.md`](agent/ORCHESTRATION-WORKLOG.md) — session-by-session history (snapshot of the live worklog, refreshed each session)
- [`runbooks/SANDBOX-RESTORE.md`](runbooks/SANDBOX-RESTORE.md) — zero-to-resumed procedure after a sandbox wipe + the session-end backup rule
- [`runbooks/AGENT-BOOTSTRAP-PROMPT.md`](runbooks/AGENT-BOOTSTRAP-PROMPT.md) — the paste-once prompt to bootstrap a fresh agent
- [`runbooks/LOCAL-PC-RUNNER.md`](runbooks/LOCAL-PC-RUNNER.md) — the owner's one-double-click launcher (credentials template is a clean paste zone since R47 — no key material ships with it)
- [`runbooks/PUBLIC-DASHBOARD.md`](runbooks/PUBLIC-DASHBOARD.md) — the public status dashboard (separate repo), its allow/deny lists and publish flow
- [`runbooks/COMPUTER-USE.md`](runbooks/COMPUTER-USE.md) — **the desktop-control system (R61, R66/R67/R68/R69-patched, R70-adjacent)**: setup per platform, the settings gates (master switch / postures / readiness — the vision model moved to its own section), the big-apps find_elements workflow + the R66 Windows walk + the R68 Chromium poke (Edge's web tree searchable), the R67 key-tool table (R68: SendInput, the win key works) + the Tab-walk readback + the monitor turn-holds, the R68 foreground self-heal + activate ladder + 30 s frames + ARGV ceiling + vision retry, the R69 enforcement layer (observation receipts + returnState + click verification + frame_changed/screen_unchanged + the aHash frame memory + element middle/right clicks), the top-center 460×56 monitor (R68: invisible to captures; R69: never steals focus) + STOP + the decayed live signal, the safety contract, the refusal catalog, the 31-tool reference, the R70 note (the engine untouched — the prompt section points at the sticky skill body; env grounding + verify-before-done discipline), and the LIVE-VERIFICATION CHECKLIST (the backends are not yet live-verified)
- [`runbooks/EMBEDDED-BROWSER.md`](runbooks/EMBEDDED-BROWSER.md) — **the in-app browser panel (R66/R67)**: the 15-action `browser_control` surface (which actions need the native bridge), the R67 binding model (one chat session, one tab + per-project cookies), the instant navigate frame + the WebView2 eval decoder, the form-submission discipline, the bot-wall checkpoint protocol (detect → the countdown card → REST resolve → the honest re-probe), the instant viewport apply, and read_dom/source — the page without screenshots
- [`runbooks/DEBUG-MODE.md`](runbooks/DEBUG-MODE.md) — **the post-turn context-free debug analyst (R66/R67/R68)**: the C1 contract (no self-report), the flow (turn → debug-start → live deltas → persisted `debug.report` → done), what the analyst receives, the isolation guarantees (follow-ups never see it), the R67 card copy + auto-collapse + the debug-gated full-turn copy, the R68 inline screenshot capture rows + their export marker, the switch, and the honest limitations
- [`runbooks/ATTACHMENTS.md`](runbooks/ATTACHMENTS.md) — **the chat attachment pipeline (R67)**: the four composer sources (drop / paste / OS picker / @ mention), the `POST /attachments/upload` contract (modes, caps, dedupe-never-overwrite), the model-facing analyze_image path contract, the picker's 512 KB caveat, and the ephemeral live-screenshot rasters (the separate registry + route + TTL — consumed by the chat's R68 INLINE capture rows)
- [`runbooks/EXTENSIBILITY.md`](runbooks/EXTENSIBILITY.md) — **the four extension surfaces (R61, + the R66 + R70 + R71 + R72 + R73/R75 addenda)**: external .mjs plugins, skills (SKILL.md-style, progressive disclosure — DB rows AND files on disk since R70: .acute/skills + ~/.agents/skills, 20 builtins with trigger-rich descriptions since R71/R72/R73, references/ depth + the task-hints advisory line since R72, sticky bodies, the agent.skills allowlist), MCP servers (stdio JSON-RPC, sanitized env), and the built-in plugin catalog — with working examples of each; the R73 addendum adds the MODES sibling tier (the six postures + .acute/agents/*.md customs with shadowing + tool narrowing, switch_mode, the picker + /mode); the R75 addendum adds the hard enforcement (mode-policy intersections, owner-pinned switch_mode, delegation inheritance)

## Requirements & architecture
- [`specs/SPEC.md`](specs/SPEC.md) — master requirements (F1–F11), owner-approved
- [`architecture/ARCHITECTURE.md`](architecture/ARCHITECTURE.md) — system design truth (topology, lifecycles, key hand-off) — *partially historical; see IMPLEMENTED-API for the shipped surface*
- [`architecture/api/IMPLEMENTED-API.md`](architecture/api/IMPLEMENTED-API.md) — **the REST + SSE surface as actually shipped** (verified against code; each round's additions are sections — R61 computer-use/skills/MCP/plugins routes + the `computer-use` SSE frame, R62 the browser-command bridge, R65 the debug switch + browser auto-open, R66 the checkpoint resolve + `/vision` routes + the new SSE frames + `debug.report` + `analyze_image`, R67 the attachments upload + browser bind + frames raster routes, the navigate/open/screenshot frames, and the binding-scoped browser_control semantics, R68 the screenshot frame's inline consumer contract + the computer-use receipt/refusal payload updates, R69 the observation-receipt payloads + the new emitters/refusal codes, R70 the skills merged listing + file-skill 409s + read_skill gating + read_file's offset/limit params, R71 the PROVIDER_ERROR class fields + the `meta.overflow_recovery` SSE frame, R72 the `GET /skills` references metadata + read_skill's `reference` param + the read_file conventions reminder, R73 the `PATCH /sessions/:id` activeMode field + `GET /projects/:id/modes` + the switch_mode tool + the mode-facing prompt sections, R74 no REST change (launcher-only), R75 the `GET /projects/:id/modes` `readOnly` field + the `meta.retry` SSE frame + `attempts` on the 502/`turn.error` payloads + the task-mode enforcement + the sync-path honest 502 + the INTERRUPTED boot sweep, R78 the queue routes (POST/DELETE `/sessions/:id/queue`) + `GET/PUT /settings/retry` + the `user.queued`/`queued.delivered`/`meta.queue_continue` frames + `meta.retry`'s `providerError` + the real-text `classMessage` semantics + the `message.queued` event/flip + the queue-continuation behavior, R79 the `delegate_task` tool's task_id/background/resume semantics + the `taskId` field on the `/subagents` rows and the `subagent-status` frames + the `delegation.collected` event + the model-facing BACKGROUND TASKS reminder contract)
- [`architecture/api/API.md`](architecture/api/API.md) — the full 52-operation *planned* contract (aspirational since Phase 1; WS/planned routes live here, NOT in IMPLEMENTED-API)
- [`architecture/PROJECT-MAP.md`](architecture/PROJECT-MAP.md) — the product map & naming rules
- [`architecture/PILLARS.md`](architecture/PILLARS.md) — the three-pillar blueprint (coding / agentic system / automation) and how they interconnect additively
- [`decisions/`](decisions/) — ADRs 0001–0028 + `TEMPLATE.md`; sequential, never renumbered; next = 0029 (ADR-0028 the background-delegation + task-addressing tier, R79)

## Design
- [`design/ui-direction.md`](design/ui-direction.md) — the owner's design language, derived from his demos
- [`design/DESIGN-SYSTEM.md`](design/DESIGN-SYSTEM.md) — living UI reference: tokens, spacing, borderless chat language, motion, anatomy, new-screen checklist
- `design/demos/` — the owner's original demos (frozen reference; never imported by product code)

## UI iteration rounds
- [`ui-iterations/README.md`](ui-iterations/README.md) — the round method + per-screen status board (owner directive 2026-08-22)
- `ui-iterations/round-NN.md` + `assets/round-NN/` — one file per owner review round with screenshots and verification evidence

## Plans & reviews (historical once the phase closes)
- `runbooks/plan-phase-0/1/2/3.md`, `plan-ui-fidelity.md`, `plan-agentic-mvp.md` — intent documents
- `runbooks/review-phase-1.md`, `review-phase-2.md` — phase close-out reports
- `runbooks/SETUP.md` — Phase-0 environment snapshot (current environment facts live in HANDOFF §8 + SANDBOX-RESTORE)
- `runbooks/DEMO.md` — demo script

## Research
- [`research/README.md`](research/README.md) — index + synthesis of the 10 reference analyses (incl. n8n for the automation pillar)
- `research/<project>/` — per project: `README.md` + `architecture.md` + `patterns-for-acute-code.md`

## Status & compliance
- [`status.json`](status.json) — hand-maintained input for the public dashboard (schema `acute-status/1`)
- [`compliance/dependency-licenses.md`](compliance/dependency-licenses.md) — **generated** by `pnpm license:audit`; never hand-edit

## Folder-structure rules (binding)
1. SQL lives only in `agent-core/src/storage/`; migrations in `src/storage/migrations/` are numbered and append-only (never edit an applied migration).
2. Unit tests: `agent-core/tests/*.test.ts` (AI SDK mocked — never live calls) and frontend suites in `src/`. Black-box E2E: `tests/e2e/` (runs against built dist).
3. `shared/` holds canonical domain types; product code conforms, never duplicates.
4. ADRs: `docs/decisions/NNN-kebab-slug.md`, zero-padded, strictly sequential, via `TEMPLATE.md`.
5. Rounds: `docs/ui-iterations/round-NN.md` (next free number; session number ≠ file number — record the mapping inside the file); screenshots in `assets/round-NN/`.
6. Research: one folder per project, always the README/architecture/patterns trio; indexed in `research/README.md`.
7. `design/demos/` is frozen owner reference — lint-excluded, never imported.
8. Scripts: repo tooling in `scripts/` (stdout = data, stderr = diagnostics). `launcher/` is owner-facing; `.bat` files must be CRLF-verified and tiny coordinators only.
9. Generated files are marked and never hand-edited.
10. Nothing secret in the repo, ever; `.dev/` DBs and `.env*` are gitignored; `.env.development` is deleted before any verify/commit.
