{
  "name": "agent-core",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist && node -e \"require('node:fs').cpSync('src/storage/migrations', 'dist/storage/migrations', { recursive: true })\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.34",
    "ai": "^7.0.73",
    "better-sqlite3": "^13.0.3",
    "fastify": "^5.12.1",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  },
  "main": "dist/main.js"
}
