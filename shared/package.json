{
  "name": "shared",
  "version": "0.101.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit false --rootDir src --outDir dist --declaration",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
