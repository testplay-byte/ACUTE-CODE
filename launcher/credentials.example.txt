# ACUTE-CODE launcher credentials — LOCAL FILE, NEVER UPLOADED.
#
# 1. Rename this file to:  credentials.txt
# 2. Open it in Notepad and paste your two values on the lines below.
# 3. Save. Double-click ACUTE.bat.
#
# This file stays on YOUR PC. The launcher reads it to download the private
# GitHub repository and to enable live model calls. Rotate or clear the values
# here whenever you like — nothing else needs to change.
#
# ─── fill these two lines ────────────────────────────────────────────────

GITHUB_PAT=github_pat_PASTE_YOURS_HERE

OPENROUTER_KEY=sk-or-v1-PASTE_YOURS_HERE

# ─── sub-agent pool keys (OPTIONAL — ROUND-44) ─────────────────────────────
# These three keys are used by SUB-AGENTS first, so your main key above is
# not burdened by parallel child runs. They appear automatically in
# Settings → Sub-agents as pool slots 2/3/4 — no manual pasting needed.
# The launcher auto-adds them to your credentials.txt if missing; edit or
# remove the lines any time (children then fall back to the main key).

OPENROUTER_SUB1_KEY=REDACTED-OPENROUTER-KEY-PURGED-BEFORE-PUBLIC-MIGRATION

OPENROUTER_SUB2_KEY=REDACTED-OPENROUTER-KEY-PURGED-BEFORE-PUBLIC-MIGRATION

OPENROUTER_SUB3_KEY=REDACTED-OPENROUTER-KEY-PURGED-BEFORE-PUBLIC-MIGRATION

# ─── where to get them ───────────────────────────────────────────────────
#  GITHUB_PAT      a GitHub Personal Access Token with read access to the
#                  private repo testplay-byte/ACUTE-CODE
#                  (github.com → Settings → Developer settings → Tokens)
#  OPENROUTER_KEY  your OpenRouter API key (openrouter.ai → Keys);
#                  starts with sk-or-v1-
#  OPENROUTER_SUBn_KEY  additional OpenRouter keys for the sub-agent pool
#                  (optional — same place as the main key)
#
# Lines starting with # are comments and are ignored.
